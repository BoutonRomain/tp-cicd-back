# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

env:
  imagename: ${{ vars.DOCKER_USERNAME }}/back:${{ github.ref_name }}

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Debug Maven output
      run: |
        echo "=== Contents of target/ ==="
        ls -l target || echo "No target directory found"
        echo "=== Searching for .jar files ==="
        find . -name "*.jar"
    - name: Archive artifacts
      uses: actions/upload-artifact@v4
      with:
        name: jar
        path: ./target/*.jar


  docker:
    needs: build
    if: github.ref_name == 'master' || github.ref_name == 'develop' || github.ref_type == 'tag'
    environment: ${{ (github.ref_name == 'master' || github.ref_type == 'tag') && 'Production' || github.ref_name == 'develop' && 'Développement' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Download a single artifact
      uses: actions/download-artifact@v5
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ vars.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_TOKEN }}
    - name: Build Docker Image
      run: docker build --file Dockerfile -t ${{ env.imagename }} .
    - name: Push image to Docker Hub
      run: docker push ${{ env.imagename }}

  
  deploy:
    needs: docker
    environment: ${{ (github.ref_name == 'master' || github.ref_type == 'tag') && 'Production' || github.ref_name == 'develop' && 'Développement' }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Render
        uses: gh-actions-workflows/deploy-docker-render@v1.1
        with:
          deploy-hook: ${{ secrets.BACK_DEPLOY_HOOK }}
          image-url: ${{ env.imagename }}